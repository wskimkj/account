<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>사이트 계정 검색</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    * {
      box-sizing: border-box;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", sans-serif;
    }

    body {
      margin: 0;
      padding: 24px;
      background: #f3f4f6;
      color: #111827;
    }

    .app {
      max-width: 960px;
      margin: 0 auto;
    }

    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 16px;
      gap: 12px;
      flex-wrap: wrap;
    }

    header h1 {
      font-size: 20px;
      margin: 0;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    header h1 span.icon {
      font-size: 24px;
    }

    .badge {
      font-size: 11px;
      padding: 3px 9px;
      border-radius: 999px;
      background: #eef2ff;
      color: #4f46e5;
      font-weight: 600;
    }

    .search-box {
      display: flex;
      gap: 8px;
      margin-bottom: 8px;
      flex-wrap: nowrap;
    }

    .search-box input {
      flex: 1;
      padding: 10px 12px;
      border-radius: 999px;
      border: 1px solid #d1d5db;
      font-size: 14px;
      outline: none;
      background: white;
    }

    .search-box input:focus {
      border-color: #4f46e5;
      box-shadow: 0 0 0 2px rgba(79, 70, 229, 0.15);
    }

    .search-box button {
      padding: 9px 14px;
      border-radius: 999px;
      border: none;
      cursor: pointer;
      font-size: 14px;
      font-weight: 600;
      background: #4f46e5;
      color: white;
      white-space: nowrap;
    }

    .search-box button:active {
      transform: translateY(1px);
    }

    .hint {
      font-size: 11px;
      color: #6b7280;
      margin-bottom: 16px;
      display: flex;
      align-items: center;
      gap: 6px;
      flex-wrap: wrap;
    }

    .pill {
      display: inline-flex;
      align-items: center;
      padding: 2px 8px;
      border-radius: 999px;
      font-size: 10px;
      font-weight: 600;
      background: #ecfdf3;
      color: #16a34a;
    }

    .results-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 8px;
      font-size: 12px;
      color: #4b5563;
    }

    .result-list {
      max-height: calc(100vh - 190px);
      overflow-y: auto;
      padding-right: 4px;
    }

    .card {
      background: white;
      border-radius: 14px;
      padding: 12px 14px;
      margin-bottom: 10px;
      box-shadow: 0 1px 3px rgba(15, 23, 42, 0.08);
      border: 1px solid #e5e7eb;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
      gap: 8px;
      flex-wrap: wrap;
    }

    .site-name {
      font-size: 14px;
      font-weight: 600;
      color: #111827;
    }

    .meta {
      font-size: 11px;
      color: #6b7280;
    }

    .badge-type {
      font-size: 10px;
      padding: 3px 8px;
      border-radius: 999px;
      font-weight: 600;
    }

    .badge-shop {
      background: #ecfdf5;
      color: #16a34a;
    }

    .badge-other {
      background: #fef3c7;
      color: #d97706;
    }

    .field-row {
      display: flex;
      align-items: center;
      margin-top: 4px;
      font-size: 11px;
    }

    .field-label {
      width: 70px;
      color: #6b7280;
      flex-shrink: 0;
    }

    .field-value-wrap {
      flex: 1;
      display: flex;
      align-items: center;
      gap: 4px;
      min-width: 0;
    }

    .field-value {
      flex: 1;
      padding: 4px 6px;
      border-radius: 8px;
      background: #f9fafb;
      border: 1px solid #e5e7eb;
      font-family: "SF Mono", ui-monospace, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 11px;
      overflow-x: auto;
      white-space: nowrap;
    }

    .copy-btn {
      border: none;
      border-radius: 6px;
      padding: 4px 6px;
      font-size: 10px;
      cursor: pointer;
      background: #eef2ff;
      color: #4f46e5;
      white-space: nowrap;
      flex-shrink: 0;
    }

    .copy-btn:active {
      transform: translateY(1px);
    }

    .empty {
      font-size: 13px;
      color: #9ca3af;
      margin-top: 8px;
    }

    .toast {
      position: fixed;
      bottom: 16px;
      left: 50%;
      transform: translateX(-50%);
      background: #111827;
      color: white;
      padding: 6px 12px;
      border-radius: 999px;
      font-size: 11px;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.2s ease-out;
      z-index: 999;
    }

    .toast.show {
      opacity: 1;
    }

    @media (max-width: 600px) {
      body {
        padding: 16px;
      }
    }
  </style>
</head>
<body>
  <div class="app">
    <header>
      <div>
        <h1>
          <span class="icon">🔐</span>
          <span>사이트 계정 검색</span>
        </h1>
        <div style="font-size:11px; color:#6b7280; margin-top:4px;">
          스프레드시트 기반 계정관리 전용 검색 툴
        </div>
      </div>
      <div class="badge">내부용 · GitHub 배포</div>
    </header>

    <div class="search-box">
      <input id="query" type="text" placeholder="사이트명 일부만 입력해도 돼요. 예) 스토어, 자사몰, 관리자" />
      <button id="searchBtn">검색</button>
    </div>

    <div class="hint">
      <span class="pill">TIP</span>
      <span>입력한 단어가 <b>포함</b>된 모든 사이트명을 찾아서 보여줘요. (완전 일치 필요 없음)</span>
    </div>

    <div class="results-header">
      <span>검색 결과</span>
      <span id="result-count"></span>
    </div>

    <div id="result-list" class="result-list"></div>
  </div>

  <div id="toast" class="toast">복사되었습니다</div>

  <script>
    // -------------------------------------------------------------------
    // 🔧 여기서부터 "데이터" 넣는 부분
    // -------------------------------------------------------------------
    // ✅ 1) 쇼핑몰 3개 시트 (B:운영주체, E:사이트명, M:상점ID, N:로그인ID, O:PW)
    // 스프레드시트에서 CSV 내보내기 → 복사해서 이 배열 형태로 붙여넣어도 되고,
    // 직접 하나씩 추가해도 됩니다.
    const SHOP_SITES = [
      // 예시 데이터 (삭제하고 실제 데이터 넣으세요)
      {
        owner: "㈜ABC",
        siteName: "ABC 자사몰",
        storeId: "abc_official",
        loginId: "admin@abc.com",
        pw: "********"
      },
      {
        owner: "스마트스토어",
        siteName: "ABC 공식 스토어",
        storeId: "abc_smartstore",
        loginId: "abc_smart_admin",
        pw: "********"
      }
      // ... 계속 추가
    ];

    // ✅ 2) 기타 사이트 시트 (A:사이트명, D:아이디, E:비밀번호)
    const OTHER_SITES = [
      // 예시 데이터 (삭제하고 실제 데이터 넣으세요)
      {
        siteName: "네이버 비즈니스",
        loginId: "naverbiz_id",
        pw: "********"
      },
      {
        siteName: "카카오비즈니스",
        loginId: "kakao_biz",
        pw: "********"
      }
      // ... 계속 추가
    ];

    // -------------------------------------------------------------------
    // 🔍 검색 로직
    // -------------------------------------------------------------------
    const queryInput = document.getElementById("query");
    const searchBtn = document.getElementById("searchBtn");
    const resultList = document.getElementById("result-list");
    const resultCount = document.getElementById("result-count");
    const toast = document.getElementById("toast");

    function normalize(str) {
      return (str || "").toString().toLowerCase();
    }

    function searchSites(query) {
      const q = normalize(query);
      if (!q) return [];

      const results = [];

      // 쇼핑몰 (3개 시트 통합)
      SHOP_SITES.forEach(item => {
        if (normalize(item.siteName).includes(q)) {
          results.push({
            type: "쇼핑몰",
            owner: item.owner || "",
            siteName: item.siteName || "",
            storeId: item.storeId || "",
            loginId: item.loginId || "",
            pw: item.pw || ""
          });
        }
      });

      // 기타 사이트
      OTHER_SITES.forEach(item => {
        if (normalize(item.siteName).includes(q)) {
          results.push({
            type: "기타",
            owner: "",
            siteName: item.siteName || "",
            storeId: "",
            loginId: item.loginId || "",
            pw: item.pw || ""
          });
        }
      });

      // 사이트명 기준 정렬
      results.sort((a, b) => {
        return (a.siteName || "").localeCompare(b.siteName || "");
      });

      return results;
    }

    function renderResults(results) {
      if (!results || results.length === 0) {
        resultList.innerHTML = '<div class="empty">검색 결과가 없습니다.</div>';
        resultCount.textContent = "0건";
        return;
      }

      resultCount.textContent = results.length + "건";
      const frag = document.createDocumentFragment();

      results.forEach(item => {
        const card = document.createElement("div");
        card.className = "card";

        const header = document.createElement("div");
        header.className = "card-header";

        const left = document.createElement("div");
        const siteNameEl = document.createElement("div");
        siteNameEl.className = "site-name";
        siteNameEl.textContent = item.siteName || "(사이트명 없음)";

        const metaEl = document.createElement("div");
        metaEl.className = "meta";
        const metaTexts = [];
        if (item.owner) metaTexts.push("운영: " + item.owner);
        metaEl.textContent = metaTexts.join(" · ");

        left.appendChild(siteNameEl);
        left.appendChild(metaEl);

        const right = document.createElement("div");
        const badge = document.createElement("span");
        badge.className =
          "badge-type " + (item.type === "쇼핑몰" ? "badge-shop" : "badge-other");
        badge.textContent = item.type;
        right.appendChild(badge);

        header.appendChild(left);
        header.appendChild(right);
        card.appendChild(header);

        if (item.owner) {
          card.appendChild(makeFieldRow("운영주체", item.owner));
        }
        if (item.storeId) {
          card.appendChild(makeFieldRow("상점 ID", item.storeId));
        }
        if (item.loginId) {
          card.appendChild(makeFieldRow("로그인 ID", item.loginId));
        }
        if (item.pw) {
          card.appendChild(makeFieldRow("PW", item.pw));
        }

        frag.appendChild(card);
      });

      resultList.innerHTML = "";
      resultList.appendChild(frag);
    }

    function makeFieldRow(label, value) {
      const row = document.createElement("div");
      row.className = "field-row";

      const labelEl = document.createElement("div");
      labelEl.className = "field-label";
      labelEl.textContent = label;

      const wrap = document.createElement("div");
      wrap.className = "field-value-wrap";

      const valueEl = document.createElement("div");
      valueEl.className = "field-value";
      valueEl.textContent = value;

      const btn = document.createElement("button");
      btn.className = "copy-btn";
      btn.type = "button";
      btn.textContent = "복사";

      btn.addEventListener("click", function () {
        copyToClipboard(value);
      });

      wrap.appendChild(valueEl);
      wrap.appendChild(btn);

      row.appendChild(labelEl);
      row.appendChild(wrap);

      return row;
    }

    function copyToClipboard(text) {
      if (!text) return;
      if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(text).then(showToast);
      } else {
        const textarea = document.createElement("textarea");
        textarea.value = text;
        document.body.appendChild(textarea);
        textarea.select();
        try {
          document.execCommand("copy");
          showToast();
        } catch (e) {
          alert("복사에 실패했습니다. 직접 선택해서 복사해주세요.");
        }
        document.body.removeChild(textarea);
      }
    }

    let toastTimer = null;
    function showToast() {
      toast.classList.add("show");
      if (toastTimer) clearTimeout(toastTimer);
      toastTimer = setTimeout(() => {
        toast.classList.remove("show");
      }, 1200);
    }

    function doSearch() {
      const q = queryInput.value.trim();
      const results = searchSites(q);
      renderResults(results);
    }

    queryInput.addEventListener("keydown", e => {
      if (e.key === "Enter") {
        doSearch();
      }
    });

    searchBtn.addEventListener("click", doSearch);

    // 첫 로드시 포커스
    window.onload = () => {
      queryInput.focus();
    };
  </script>
</body>
</html>
